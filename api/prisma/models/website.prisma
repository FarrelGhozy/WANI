// Single-row store website config (id = "default").
model WebSite {
  id        String   @id @default("default")
  config    Json     @default("{}")
  published Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
